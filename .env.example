# Optional: connect to your own MongoDB. Without this, a local MongoDB process uses .data/mongo.
# MONGODB_URI=mongodb://127.0.0.1:27017
PORT=3001
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.5-flash-lite
GEMINI_BILLING_TIER=free

